# MEMORIA — D-Flight personale

> Diario di progetto + **documento di handoff** per continuare in una nuova chat.
> Ultimo aggiornamento: 2026-07-09 notte — Fase 3 in produzione; **bug offline dal test iPhone di Lorenzo ("mappa e aree non caricano") diagnosticato e FIXATO** (3 cause concorrenti: prima sessione senza clientsClaim, CARTO ha cambiato il template dei tile in giornata, worker MapLibre bypassano il SW). Suite 183/183, E2E offline **11/11 nello scenario iPhone reale** (una sola sessione online, niente reload), regressione 15/15. **Da rifare il test iPhone.**

## Cos'è
Web app (PWA) personale **non ufficiale** per visualizzare le **zone geografiche UAS italiane** (dati ufficiali D-Flight, formato ED-269) con UI moderna, e pianificare voli con verdetto personalizzato. Non sostituisce la verifica ufficiale su D-Flight.

## Decisioni chiave (brainstorming 2026-06-30)
- **Scopo:** mappa zone + pianificazione personalizzata (opzionale).
- **Funzione base:** mappa "nuda e cruda" (zone colorate + quota max in etichetta su ogni zona); pianificazione **on-demand**.
- **Device:** PWA responsive completa (desktop + mobile), offline.
- **Sync:** solo **import manuale** del file ED-269 (no backend, no credenziali). Motivo: il **CORS** impedisce il download diretto dal browser; auto-login con credenziali rimandato a eventuale **versione nativa** (Capacitor).
- **Pianificazione:** verdetto personalizzato per drone (classe C0–C4 / sub-250g) + brevetto (A1/A3, A2), calcolato in locale, **conservativo** (nel dubbio: "verifica ufficialmente").
- **Estetica:** stile "B — Chiaro & essenziale" (mappe iOS); tema **Chiaro/Scuro/Sistema**; accenti blu (`#007aff`/`#0a84ff`); colori zone rosso/arancio/giallo/verde.
- **GPS:** posizione sulla mappa (puntino blu); resta sul dispositivo.
- **Stack:** React + TS + Vite, MapLibre (CARTO Positron/Dark Matter, keyless), Tailwind v3, IndexedDB (`idb`), Vitest; geocoder **Photon**.
- **Hosting (deciso 2026-07-03):** pubblicare su **GitHub Pages** (gratis) a fine Fase 1, così l'app è usabile anche da Safari/iPhone (PWA installabile da "Aggiungi alla schermata Home").

## TODO prossima sessione
- **📱 TEST IPHONE DA RIFARE (solo Lorenzo), col fix offline deployato:** se l'app è già installata, aprirla **online** e attendere ~15s che si aggiorni (toast → Aggiorna) e riscaldi le cache; altrimenti installarla da Safari (Condividi → "Aggiungi alla schermata Home"). Poi in **modalità aereo**: shell, banner "Sei offline", **sfondo mappa dell'Italia/aree viste**, zone visibili, verdetto Verifica. Nota: alla prima apertura post-install servono ~10–15s online perché il warmup riempia le cache (stile + tile vista corrente).
- **Backlog Minor Fase 3** (review finale, tutti non bloccanti): status-bar iOS `default` su tema scuro (valutare dopo test iPhone); `theme_color` manifest fisso `#0b1828`; attributo `data-build` in prod (hook E2E); `navigator.onLine` falso-positivo possibile; lista risultati SearchBox resta visibile se si va offline con dropdown aperto; runtime cache SWR senza `maxAgeSeconds`; E2E: sleep fissi + server spawnato fuori dal try.
- **In sospeso:** `avvio.command` (chiesto da Lorenzo, tenuto) e `.claude/` restano **untracked** — decidere se committare lo script (il repo è pubblico) o tenerlo solo locale.
- **Fase 1 CHIUSA, deploy FATTO (2026-07-03):** app online su **https://fara2106.github.io/d-flight-personale/** (repo pubblico `Fara2106/d-flight-personale`). **Decisione utente (2026-07-03): il repo resta pubblico**, con la consapevolezza che pubblico = trovabile (profilo GitHub, ricerca, indicizzazione), non "solo con il link"; solo il sito Pages è di fatto non elencato. Valutato il passaggio a privato (l'utente ha Pro: il sito continuerebbe a funzionare, comando: `gh repo edit Fara2106/d-flight-personale --visibility private --accept-visibility-change-consequences` — da far eseguire all'utente), scelto di lasciarlo così. Deploy automatico a ogni push su `main` via `.github/workflows/deploy.yml` (typecheck + test + build + Pages). Nota: le deployment Pages su questo repo ogni tanto falliscono con "Deployment failed, try again later" (capitato 2 volte su 4, anche dopo la prima) — **basta rilanciare la run** (`gh run rerun <id> --failed`), al retry passa. I push solo-docs (MEMORIA, docs/) non triggerano il deploy (`paths-ignore`).
- **Prossimo lavoro grosso:** Fase 2 — **spec APPROVATA dall'utente (2026-07-04)** (`docs/superpowers/specs/2026-07-03-dflight-fase2-pianificazione-design.md`) e **piano scritto**: `docs/superpowers/plans/2026-07-04-dflight-fase2-pianificazione.md` (13 task TDD: DB v2 + profili → tabella regole + engine → intersect Turf → rework popup accordion → VerdictSheet/VerifyControls → integrazione App → E2E). Decisioni di piano nel §"Decisioni di piano" (delta conservativi: `applicabilityText` opzionale, `Verdict.subcategory`, verticalRef null conta nel min quota, fix attribution incluso, Playwright `npm i --no-save`). **Prossimo passo: eseguire il piano** (metodo subagent-driven come Fase 1 o inline — scelta utente in sospeso se la sessione si interrompe qui).
- **Fix da E2E (2026-07-03, entrambi mergiati su main):**
  1. ✅ `25421d3` — le regole popup di `index.css` **non si applicavano mai**: `maplibre-gl.css` viene dopo nel grafo dei moduli e a parità di specificità vinceva (popup bianco fisso, illeggibile in dark). Fix: prefisso `:root` su tutte le regole popup. Gotcha da ricordare per ogni override di CSS di libreria.
  2. ✅ `ee34d9c` — ricerca Photon muta: l'API ha **rimosso il supporto `lang=it`** (risponde 400; restano default/de/en/fr). Tolto il parametro `lang`.
- **Verifica E2E — COMPLETATA (2026-07-03, Playwright headless):** empty state, import, zone visibili, popup singola/multi-zona (dedup + ordinamento restrittività ok), popup dark leggibile (surface `#1f2630`, radius 12px), ricerca ("Colosseo, Roma, Lazio" → flyTo), GPS (mock, flyTo + puntino), errore file invalido mostrato e ripulito al re-import, persistenza zone + tema al reload, zero errori console. Script riusabile: era in scratchpad `e2e/run.mjs` (Playwright + file ED-269 sintetico con 2 zone sovrapposte su Roma + 1 separata).
- **Finding minore (backlog):** il bottone "Importa file zone (ED-269)" in basso a destra è parzialmente coperto dall'attribution CARTO su viewport 1280×800.

## Gotcha ambiente (sessione Cowork/sandbox, 2026-07-05)
- Le sessioni **Cowork** girano in un sandbox **Linux** con la cartella del progetto montata: (1) i `node_modules` del Mac (binari darwin) **non funzionano** lì → copiare il progetto in `/tmp` nel sandbox (senza node_modules) e fare `npm install` nella copia per girare test/build; MAI `npm install` dentro il mount (sporcherebbe i binari del Mac); (2) il sandbox **non può fare `unlink`** nel mount → `git commit` funziona ma lascia lock stantii (`HEAD.lock`, `index.lock`) che vanno **rinominati** (`mv … .bak` — il rename è permesso) prima dell'operazione git successiva; a fine sessione restano `.git/*.bak` e `tmp_obj_*` innocui da pulire sul Mac (`rm -f .git/*.bak` + `git gc`); (3) niente browser Playwright nel sandbox → E2E da rigirare sul Mac.

## Gotcha ambiente (2026-07-03)
- Il progetto vive in `~/Documents` **sincronizzata con iCloud Drive**: iCloud ha creato 17 duplicati di conflitto (`* 2.*`, es. `index 2.html`) che rompevano Vite/Tailwind con `ETIMEDOUT`, più file "dataless" (solo nel cloud). Risolto: duplicati eliminati (erano untracked), `brctl download .`, riavvio dev server. **Può ripresentarsi**: se Vite dà ETIMEDOUT o errori su file `* 2.*`, ripulire con `find . -name "* 2.*" -not -path "./node_modules/*" -delete` e `brctl download .`. Valutare in futuro lo spostamento del progetto fuori da iCloud.

## Idee future (da ragionare insieme, non in Fase 1-3)
- **Estensione ad altri paesi europei (proposta 2026-07-03, da discutere):** il formato ED-269 è standard UE — ogni paese pubblica le proprie zone UAS sul portale nazionale. Servirebbe: supporto multi-dataset (oggi un import sostituisce il precedente), ricerca luoghi non limitata all'Italia (oggi bbox Italia su Photon), e attenzione alle deroghe nazionali nel motore regole di Fase 2 (tarato su regole italiane).

## Documenti di riferimento
- **Specifica:** `docs/superpowers/specs/2026-06-30-dflight-personale-design.md`
- **Piano Fase 1 (14 task, TDD):** `docs/superpowers/plans/2026-06-30-dflight-personale-fase1-viewer.md`
- **Ledger SDD (locale, git-ignored):** `.superpowers/sdd/progress.md`

## Stato
- [x] Brainstorming + specifica — commit `8cc269c`, `c5b709b`
- [x] Piano Fase 1 — commit `6512e5b`
- [x] MEMORIA creata — commit `7eb4c49`
- [x] **Esecuzione Fase 1** — COMPLETATA e **mergiata su `main`** (2026-07-03, fast-forward a `1bf2747`; branch `feat/fase1-viewer` eliminato)
  - [x] **Task 1 — Scaffold** (commit `041f3df`, review **Approved**, solo minori)
  - [x] **Cleanup Task 1** (commit `32d027d`) — finding minori risolti
  - [x] **Task 2 — Tema** (commits `1f4f53b`+`5ffd2fa`, review **Approved**)
  - [x] **Task 3 — Tipi ED-269 + parser** (commit `97cdd0c`, review **Approved**)
  - [x] **Task 4 — Normalizzatore zone** (commit `a5cdfc0`, review **Approved**)
  - [x] **Task 5 — Diff zone** (commit `0a33f97`, review **Approved**)
  - [x] **Task 6 — Archivio IndexedDB** (commit `5d148df`, review **Approved**)
  - [x] **Task 7 — Import orchestratore + pulsante** (commit `c9c9e4a`, review **Approved**)
  - [x] **Task 8 — Trasformazioni mappa** (commit `c17d387`, review **Approved**; decisione utente: etichetta prohibited = "⛔ 0 m" fisso)
  - [x] **Task 9 — Stile mappa + MapView base** (commit `2415c06`, review **Approved**)
  - [x] **Task 10 — Render zone + popup** (commits `da43796`+`b700fe0`, review **Approved**; popup con soffitto formale AGL/AMSL, fix race zones/load)
  - [x] **Task 11 — Chrome UI** (commit `a42dc9f`, review **Approved**; legenda, banner staleness, disclaimer, empty state; wiring in App al Task 14)
  - [x] **Task 12 — Ricerca luoghi (Photon)** (commits `a4f3f66`+`d3a57e2`, review **Approved** dopo fix: bbox Italia, abort richieste stale)
  - [x] **Task 13 — Posizione GPS** (commit `4e93122`, review **Approved**, zero Critical/Important)
  - [x] **Task 14 — Integrazione App** (commits `eff00e9`+`c22285c`, review **Approved** dopo fix: flyTo stabile via stato esplicito)
  - [x] **Review finale whole-branch (opus)** — verdetto **Ready to merge**, zero Critical/Important; polish batch applicato (`2b29d82`: popup via textContent, clear errore su import ok, geo.error visibile, listener matchMedia tema system). Suite 34/34 + `tsc -b` verdi, verificati dal controller.
- [x] **FASE 1 COMPLETA (2026-07-03)** — restano: **verifica manuale E2E** (Step 5 Task 14) con l'utente, **decisione merge su main**, poi **deploy GitHub Pages**.
- [x] **Migliorie popup (tema + multi-zona)** — implementate, **verificate E2E** (con 2 fix: `25421d3` cascata CSS, `ee34d9c` Photon lang) e **mergiate su `main`** (2026-07-03, fast-forward a `ee34d9c`; branch eliminato). Suite 39/39 + tsc verdi su main.
- [x] **Deploy GitHub Pages** — FATTO (2026-07-03): https://fara2106.github.io/d-flight-personale/ — verificato live (app carica, zero errori console). **⚠️ AGGIORNAMENTO 2026-07-05: repo ora PRIVATO e Pages DISABILITATO.** Il deploy della Fase 2 è fallito su `actions/configure-pages@v5` con `HttpError: Not Found` («Get Pages site failed… verify that the repository has Pages enabled»). `gh api repos/Fara2106/d-flight-personale/pages` → 404; `gh repo view` → `isPrivate: true`. Riabilitazione tentata con `gh api -X POST …/pages -f build_type=workflow` → **422 «Your current plan does not support GitHub Pages for this repository.»** **L'assunzione della MEMORIA «l'utente ha Pro → il sito continua a funzionare da privato» era ERRATA**: GitHub serve Pages da repo privati solo su piani a pagamento idonei, e questo account non ne ha uno attivo per questo repo. **Decisione utente (2026-07-05): opzione 2 — tenere il repo privato e fare l'upgrade del piano GitHub (billing, solo l'utente).** Quando il piano sarà idoneo: `gh api -X POST repos/Fara2106/d-flight-personale/pages -f build_type=workflow` poi `gh run rerun <id> --failed` (o nuovo push). Alternativa: rendere il repo pubblico (`gh repo edit … --visibility public --accept-visibility-change-consequences`) fa funzionare Pages su qualsiasi piano.

  **RISOLTO 2026-07-05 (l'utente ha poi scelto l'opzione 1, con ok esplicito):** repo **rimesso PUBBLICO** (`gh repo edit … --visibility public --accept-visibility-change-consequences`; `isPrivate: false`), Pages **riabilitato** con `gh api -X POST …/pages -f build_type=workflow` (source = GitHub Actions, `html_url` https://fara2106.github.io/d-flight-personale/, `https_enforced: true`), poi `gh run rerun 28741262123 --failed`: **deploy VERDE** (build + configure-pages + upload-pages-artifact + deploy-pages tutti ✓). Sito verificato **HTTP 200**. **Fase 2 ORA IN PRODUZIONE.** Il repo è di nuovo pubblico (trovabile/indicizzabile) — tradeoff già accettato in passato. **Backlog action Node 20 — CHIUSO (2026-07-05, commit `7bde68a`):** `checkout`/`setup-node` bumpati a **v5** (Node 24); deploy ri-triggerato (run `28742081381`) **verde** e sito **HTTP 200** sul nuovo runtime. Resta un'annotazione Node 20 a monte di GitHub su `actions/configure-pages@v5` e `actions/upload-artifact@v4` (tirato da `upload-pages-artifact@v3`) — già all'ultimo major disponibile, **niente da aggiornare da parte nostra**, forzati a Node 24 automaticamente.
- [x] **Fase 2 — profili + motore regole + pianificazione: ESECUZIONE COMPLETA (2026-07-05)** su branch `feat/fase2-pianificazione` (base `c0de08b`). Tutti i **13 task** implementati, testati e committati (TDD, inline). Verifica finale di fase verde: **suite 100/100** (26 file), **`tsc -b` pulito**, **`npm run build` ok** (warning bundle 1.27 MB atteso — code-splitting MapLibre resta backlog), **E2E Playwright 15/15 verde** (`node e2e/run.mjs`). Restano: **review finale whole-branch (opus)** e **merge su `main`** (che triggera il deploy Pages) — da confermare con l'utente. Note esecuzione nel ledger `.superpowers/sdd/progress.md` (sezione FASE 2): 1 fix fixture del piano al Task 10 (zB ereditava `message` da `{...zA}`), 3 fix di *script* E2E al Task 13 (checkbox controllata async, form drone async, zone sub-pixel a ITALY_ZOOM=5) — nessun difetto d'app.
- [x] **Migliorie UX mappa (2026-07-05 sera, sessione Cowork) — committate su `main`, NON pushate:**
  - `0fcd0a1` **Zone sovrapposte leggibili**: rendering per severità — `fill-sort-key`/`line-sort-key` (le più restrittive disegnate sopra: nelle intersezioni domina il rosso, non l'ordine arbitrario del file), opacità per tipo (`ZONE_FILL_OPACITY` 0.32→0.12: le zone innocue sono un velo), bordi gerarchici (`ZONE_LINE_WIDTH` 2.4→1.0, opacity 0.9), `symbol-sort-key` sulle etichette (in collisione vince la quota della zona più restrittiva), nuovo layer `zones-highlight-fill` (velo blu 0.18 sulla zona aperta nell'accordion, oltre al bordo). `RESTRICTION_ORDER` esportato da `mapStyle.ts` = unica fonte dell'ordinamento (popup + rendering).
  - `5cfe5d0` **Popup in linguaggio semplice per principianti**: nuovo modulo puro `src/map/plainLanguage.ts` (`plainZoneInfo`: headline concreta per tipo + righe quota/motivo/attivazione; `parseReasons` gestisce anche i reasons serializzati JSON da MapLibre); `reasons` ED-269 ora nelle properties GeoJSON; il dettaglio del popup apre con la frase semplice (es. "Vietato far volare il drone qui", "Quota massima qui: 60 m dal suolo", "Motivo: zona aeroportuale / traffico aereo") e il gergo (quota raw, tipo ED-269, message) sta in un `<details>` "Dettagli tecnici" collassato. AMSL/WGS84 mai convertiti, con avviso "non è l'altezza dal suolo" (policy conservativa).
  - `2e9b172` **Code-splitting MapLibre (backlog CHIUSO)**: `build.rolldownOptions.output.advancedChunks.groups` in `vite.config.ts` → chunk `maplibre-*.js` separato (1.03 MB, 273 kB gzip) + suo CSS; entry dell'app **243 kB** (77 kB gzip) contro il monolite da 1.27 MB. `index.html` con `modulepreload` corretto. Il warning >500 kB ora riguarda solo il chunk vendor (atteso).
  - Verifica: TDD (rosso verificato con mutation check), **suite 117/117** (+12 test: `zoneLayers`, `plainLanguage`, popup, zonesToGeoJSON), `tsc -b` pulito, `npm run build` ok. **E2E Playwright NON rigirati** (sandbox senza browser) → da fare sul Mac. **Push in attesa dell'ok di Lorenzo.**
- [x] **Fase 3 — offline / PWA: COMPLETA E IN PRODUZIONE (2026-07-09)** — spec `docs/superpowers/specs/2026-07-05-dflight-fase3-offline-pwa.md` + piano `docs/superpowers/plans/2026-07-05-dflight-fase3-offline-pwa.md`. 6 task subagent-driven + Task 8 cache tile (decisione A rivista), review finale opus **Ready to merge**. Merge fast-forward su `main` (`ea75314..d59c2a3`), deploy Pages verde (run `29045504711`), live verificato con Playwright (SW attivo, manifest, zero errori console). Branch eliminato. Resta solo il **test iPhone** di Lorenzo.

---

## RIPRESA — istruzioni per la nuova chat

**Metodo:** subagent-driven-development (superpowers). Nella nuova chat di' qualcosa come:
> "Continua l'esecuzione del piano `docs/superpowers/plans/2026-06-30-dflight-personale-fase1-viewer.md` col metodo subagent-driven, riprendendo dal **Task 2** (il Task 1 è già completo, vedi `.superpowers/sdd/progress.md`). Branch `feat/fase1-viewer`."

**Stato git:** tutto su `main` (Fase 1 mergiata il 2026-07-03, branch eliminato). La sezione "Ciclo per ogni task" qui sotto resta come riferimento di metodo per i piani di Fase 2/3.

**Ciclo per ogni task** (Task N):
1. `task-brief` → estrai il brief: `<SK>/scripts/task-brief docs/superpowers/plans/2026-06-30-dflight-personale-fase1-viewer.md N`
2. Registra BASE = HEAD attuale, poi **dispatch implementer** (modello sotto) col brief + file di report.
3. `review-package BASE HEAD` → **escludi `package-lock.json`** dal diff (è enorme): `git diff -U10 BASE..HEAD -- . ':(exclude)package-lock.json'`.
4. **Dispatch task reviewer** (sonnet) con brief + report + diff + vincoli globali.
5. Fix solo Critical/Important; i Minor vanno nel ledger per la review finale.
6. Marca il task completo nel **ledger** e in **MEMORIA**.

`<SK>` = `/Users/lorenzofaraoni/.claude/plugins/cache/claude-plugins-official/superpowers/6.0.3/skills/subagent-driven-development`

**Modello consigliato per task** (turni bassi = meno costo):
- Trascrizione pura (codice completo nel piano) → **haiku**: Task 2,3,4,5,7,8,11,12,13
- Quirk d'ambiente / integrazione → **sonnet**: Task 6 (fake-indexeddb), 9 e 10 (MapLibre), 14 (integrazione)
- Task reviewer → **sonnet** · Review finale whole-branch → **opus**

**Risoluzioni già applicate nel Task 1 (non rifare):**
- Scaffold creato via sottocartella `.vite-tmp` poi spostata (la dir non era vuota).
- **Tailwind pinnato a v3** (il piano usa sintassi v3).
- `vite.config.ts` importa `defineConfig` da **`vitest/config`** (TS6/Vite8 non espone `test` su `vite`).

**Finding minori del Task 1 — RISOLTI** nel commit di cleanup `32d027d`:
- `index.html`: titolo → `D-Flight personale`; `lang="it"`. ✅
- Rimossi file scaffold inutilizzati (`src/App.css`, `src/assets/*`, `public/icons.svg`). ✅ (`public/favicon.svg` tenuto: referenziato da index.html.)
- `"vitest/globals"` in `tsconfig.app.json`: **non applicato** di proposito — i test non sono in `tsconfig.app` (`include:["src"]`) e Vitest non typecheck-a; lo esporrebbe a `src` senza utilità.

**Gotcha tsconfig (passare a OGNI implementer):** `verbatimModuleSyntax:true` → import di soli tipi con `import type`/`import { type X }` (es. Task 2 `ThemeToggle`: `import { ThemePref }` va corretto in `import { type ThemePref }`); `noUnusedLocals`/`noUnusedParameters` su `src`; i test del task possono passare senza coprire tutti i file `src`, quindi far eseguire anche `npx tsc -b` e riportarne l'esito.

---

## Log
- **2026-07-09 notte (bug offline iPhone → fix a 3 strati)** — Lorenzo dal test iPhone: "online ok, offline non carica la mappa e neanche le aree". Debug sistematico con repro Playwright (1 sola visita online senza reload → offline): **riprodotto subito**. TRE root cause concorrenti: (1) **niente `clientsClaim`** → al primo avvio la pagina non è controllata dal SW e le cache runtime restano vuote; offline `style.json` manca → MapLibre non inizializza → né sfondo né zone (le zone si disegnano solo dopo il load dello stile); (2) **CARTO ha cambiato il template dei tile IN GIORNATA** (rollout CDN): da `tiles.basemaps.cartocdn.com/vector/…` a `tiles-{a..d}.basemaps.cartocdn.com/vectortiles/…` → `MAP_TILE_URL_RE` non matchava più nulla e `carto-tiles-v1` non veniva mai creata (per questo l'E2E era verde al mattino e l'iPhone rotto la sera!); (3) **i tile li fetchano i worker MapLibre**: creati pre-claim bypassano il SW anche dopo il claim e le loro richieste NON compaiono nella Performance timeline della pagina. Fix (TDD, red verificati): `clientsClaim: true` in vite.config; `src/pwa/warmMapCache.ts` con `warmMapCacheOnceControlled` (replay al claim delle risorse main-thread — style/sprite/glyph/tiles.json — dalla Performance timeline, chiamato in `main.tsx`), `tileUrlsForView` (slippy math z=floor(zoom) clampato 0–14, cap 32 tile, **stesso shard di MapLibre**: `templates[(x+y)%len]`, `tile_id.ts`) e `warmVisibleTiles` (fetch main-thread dei tile della vista, solo in prima sessione, cablato in MapView su `once('idle')` + `moveend`); regex CARTO allargate a entrambe le varianti host/path (test con gli URL reali). E2E `offline.mjs` riscritto sullo scenario iPhone: niente reload online, check `fromServiceWorker()` per style e tile offline. **Suite 183/183, tsc pulito, E2E offline 11/11, regressione 15/15.** GOTCHA: mai validare i pattern URL CDN con assunzioni/curl sui metadati — guardare le richieste REALI del browser (il template sta DENTRO tiles.json e può cambiare senza preavviso).
- **2026-07-09 sera (merge Fase 3 + deploy)** — "Continua il lavoro": rifatte tutte le verifiche sul Mac con evidenza fresca — suite **166/166**, `tsc -b` pulito, build ok (precache 14 entry), **E2E offline 9/9** (incluse cache tile e toast update) e **regressione 15/15**. Merge **fast-forward** di `feat/fase3-offline-pwa` su `main` (`ea75314..d59c2a3`, coi 2 commit docs locali) + push. Deploy Pages **verde al primo tentativo** (run `29045504711`). Live verificato con Playwright headless: titolo ok, **service worker attivo**, link manifest presente, **zero errori console**; `manifest.webmanifest` e `sw.js` rispondono 200. Branch eliminato (mai esistito su origin). **Resta: test iPhone di Lorenzo** (vedi TODO). Backlog Minor Fase 3 invariato nel TODO.
- **2026-07-05 notte (pagina bianca chiarita + avvio.command robusto + tracking posizione)** — Lorenzo segnalava "pagina bianca" in dev: **nessuna regressione dai commit odierni**. Diagnosi nel sandbox: dev server pulito, tutti i moduli trasformati e le deps pre-bundlate rispondono 200, zero import runtime in `mapStyle`/`plainLanguage` (nessun ciclo da `RESTRICTION_ORDER`), expression dei layer **validate con `@maplibre/maplibre-gl-style-spec`** (0 errori), `advancedChunks` non tocca il dev (config sotto `build.`). Causa reale: URL aperto **senza il base path** `/d-flight-personale/` (o porta diversa da quella attesa); nota: `GET /` in dev risponde comunque **302 → `/d-flight-personale/`**. Confermato da Lorenzo: con l'URL corretto l'app si vede. **`avvio.command` riscritto robusto** (resta untracked): PATH per shell non interattive (Homebrew/nvm/volta), **base path auto-rilevato da `vite.config.ts`**, riuso del server già attivo (riconosciuto dal titolo), porta occupata → prima libera con `--strictPort`, browser aperto **solo** quando il server risponde davvero, errori espliciti con finestra che resta aperta, `bash -n` ok. **Tracking posizione in tempo reale** (`347d589`, richiesta di Lorenzo): `useGeolocation` esteso con `start`/`stop`/`watching` su `watchPosition` (alta precisione, `heading` normalizzato null se NaN), `geoErrorMessage` con messaggi italiani concreti (permesso negato → dice come riabilitarlo; permesso negato ferma il watch, timeout no), `LocateButton` ora è un **toggle** (aria-pressed, accent quando attivo), marker aggiornato **in place** (niente ricreazione DOM a ogni fix) con **freccia direzione** quando l'heading c'è (`.user-dot`/`.user-dot-heading` nei token tema), camera centrata **solo alla prima fix** (niente re-centering continuo, protetto il fix flyTo di Fase 1). "Usa la mia posizione" nella verifica ora riceve la posizione live (bonus già cablato). Suite **128/128** (+11), `tsc -b` pulito, build ok. Cleanup post-sessione sul Mac invariato: `rm -f .git/*.bak`.
- **2026-07-05 sera (sessione Cowork: UX mappa + code-splitting + bozza Fase 3)** — All'apertura trovate **modifiche non committate NON richieste** che rimuovevano l'integrazione Fase 2 da `App.tsx` (+ snellimenti test/E2E), in contrasto con la MEMORIA: **scartate su decisione di Lorenzo** (ripristino da HEAD; `avvio.command`, chiesto da lui, tenuto ma lasciato untracked). Poi, su richiesta: (1) **zone sovrapposte leggibili** (`0fcd0a1`) e (2) **popup in linguaggio semplice per principianti** (`5cfe5d0`) — dettagli in §Stato; TDD, suite 117/117, tsc pulito. Con l'ok di Lorenzo (da telefono, senza verifica visiva): (3) **code-splitting MapLibre** (`2e9b172`, entry 243 kB); (4) **bozza spec+piano Fase 3 offline/PWA** in `docs/superpowers/` (decisioni aperte A–D da validare); (5) questa MEMORIA. **NIENTE push: i commit restano locali finché Lorenzo non verifica visivamente** (avvio.command → import ED-269 → zone impilate + popup nei due temi) **e non rigira gli E2E sul Mac.** Lavoro svolto dal sandbox Linux (vedi nuovo gotcha ambiente: npm in copia /tmp, lock git da rinominare, niente Playwright).
- **2026-07-05 (esecuzione Fase 2)** — Ripresa da MEMORIA + ledger + piano. Eseguiti **inline (TDD)** i 13 task di Fase 2 su `feat/fase2-pianificazione` (Task 1-3 erano già committati da una sessione precedente ma non annotati nel ledger — chiusi). Ogni task: test → impl → `npx vitest run <file>` → suite completa → `npx tsc -b` → commit conventional, con annotazione nel ledger. **Suite finale 100/100, `tsc -b` pulito, `npm run build` ok** (bundle 1.27 MB, code-splitting resta backlog). **E2E Playwright 15/15 verde** (`node e2e/run.mjs`). Trovato **1 difetto di fixture nel piano** (Task 10 VerdictSheet: `zB = {...zA, id, name}` ereditava `message`, rendendo l'assert accordion non falsificabile → dato `message: 'Nota B'`) e **3 race di *script* E2E** (Task 13, nessun difetto d'app): checkbox attestato controllata async (`.check()` ri-toggle → click singolo + poll `isChecked()`); form drone `submit()` async (il `setDraft(EMPTY)` pendente svuotava il Nome del 2° drone → attendere il radio `Attiva X` tra un add e l'altro); zone fixture sub-pixel a `ITALY_ZOOM=5` (il click non colpiva `zones-fill`, hit-test a pixel → zoom con la rotella prima del click popup; la modalità verifica funziona comunque perché `zonesAtPoint` è geometrica). **Prossimi passi: review finale whole-branch (opus) → merge su `main`** (il push triggera il deploy Pages; se fallisce `gh run rerun <id> --failed`).
- **2026-07-04 (piano Fase 2)** — Utente ha approvato la spec Fase 2 (sezioni architettura/motore/errori/test riassunte e validate). Scritto il **piano di implementazione** con writing-plans: `docs/superpowers/plans/2026-07-04-dflight-fase2-pianificazione.md` — 13 task TDD bite-sized con codice completo (Task 1-4 profili+DB v2, 5 applicabilityText, 6-7 tabella regole+engine, 8 intersect Turf, 9 rework popup accordion+highlight, 10 VerdictSheet, 11 modalità verifica, 12 integrazione App + fix attribution, 13 E2E Playwright con fixture su ITALY_CENTER). Self-review fatta (fix: selettore banner E2E, assert su input controllato, ambiguità strict-mode dei message duplicati nelle note, fence markdown annidate). **Prossimo passo: esecuzione del piano** (consigliato subagent-driven come in Fase 1; modelli per task: haiku per trascrizione pura, sonnet per Task 9/11/12 MapLibre e integrazione, reviewer sonnet, review finale opus).
- **2026-06-30** — Brainstorming completato; specifica e piano scritti e committati. Avviata esecuzione subagent-driven Fase 1 su `feat/fase1-viewer`. **Task 1 (Scaffold) completato e approvato** (`041f3df`). Sessione fermata su richiesta per proseguire in una nuova chat usando questa MEMORIA come handoff.
- **2026-06-30 (ripresa)** — Nuova chat: letta MEMORIA + ledger + piano. Pre-flight plan review: nessun conflitto bloccante (solo quirk d'ambiente già previsti). **Cleanup Task 1 committato** (`32d027d`): titolo/lingua it, rimossi file scaffold; typecheck `tsc -b` verde e test verdi. Ripresa esecuzione subagent-driven dal Task 2.
- **2026-07-03** — Sessione "finisci il lavoro": completati e approvati **Task 11** (`a42dc9f`), **Task 12** (`a4f3f66`+`d3a57e2`, fix bbox Italia + abort stale), **Task 13** (`4e93122`). **Task 14** implementato (`eff00e9`, suite 32/32 verde); review ha trovato 1 Important plan-mandated (fallback `flyTo` instabile in App: re-centering spurio a ogni render + 📍 non ricentra dopo una ricerca) → fix in corso, poi re-review e review finale whole-branch (opus). Decisioni utente di oggi: hosting **GitHub Pages** a fine Fase 1; idea futura multi-paese ED-269 annotata; import resta manuale (ok utente). Dopo la Fase 1 resta la verifica manuale E2E (Step 5 del Task 14) da fare con l'utente.
- **2026-07-03 (migliorie popup)** — Ripulite di nuovo le cartelle duplicate iCloud (`src/* 2`, `tests/* 2`, vuote — il gotcha si è ripresentato). Scritte mini-spec e piano (`docs/superpowers/{specs,plans}/2026-07-03-popup-tema-multizona.md`), eseguito il piano **inline** (3 task piccoli, TDD) su branch `feat/popup-tema-multizona`: CSS popup nei token tema (`9db7dbf`), `buildPopupContent` pura con 5 test (`f59be9d`), wiring MapView con tutte le feature del click (`102f761`). Review medium sul diff del branch: **zero finding**. Suite 39/39 + `tsc -b` verdi. Chiuso il Minor "popup formatting non testato" nel ledger. **Prossimi passi:** E2E utente (popup dark + zone sovrapposte, poi ricerca/GPS/temi/persistenza) → merge su `main` → deploy GitHub Pages.
- **2026-07-03 (deploy)** — Confermato dall'utente il repo pubblico: creato `Fara2106/d-flight-personale`, aggiunti `base: '/d-flight-personale/'` in `vite.config.ts` e workflow Pages (`581cf4c`), push. Prima deployment fallita (race provisioning Pages su repo nuovo), risolta rilanciando la run. **Sito live e verificato con Playwright: https://fara2106.github.io/d-flight-personale/** (200, app carica, empty state ok, zero errori). **FASE 1 COMPLETAMENTE CHIUSA.** Backlog noti: bundle 1.24 MB (code-splitting MapLibre), bottone import coperto dall'attribution CARTO.
- **2026-07-03 (E2E + merge popup)** — Verifica E2E completa con Playwright headless (l'estensione Claude-in-Chrome non era connessa; browser Playwright già in cache). **Trovati e corretti 2 bug reali:** (1) le regole tema del popup non si applicavano mai — `maplibre-gl.css` vince la cascata perché importato dopo `index.css`; fix con prefisso `:root` (`25421d3`); (2) ricerca Photon rotta a monte — `lang=it` non più supportato, API risponde 400; tolto il parametro (`ee34d9c`, TDD). Re-run E2E: tutto verde (popup dark leggibile, multi-zona ordinato, ricerca, GPS, persistenza, errori import, zero errori console). **Merge fast-forward su `main`** (a `ee34d9c`), branch eliminato, suite 39/39 + `tsc -b` verdi su main. `gh` è autenticato (account Fara2106). **Resta solo il deploy GitHub Pages** (attende conferma utente: nome repo, visibilità).
- **2026-07-03 (brainstorming Fase 2)** — Brainstorming Fase 2 completato (sezioni 1-3 approvate dall'utente, sezione 4 presentata con utente AFK). Spec scritta, self-review fatta (fix: riga "legacy <250g" → si usa la classe sub-250, niente enum dedicato), committata (`1a0d615`). Scope: verifica punto+cerchio, profili multi-drone senza preset, pilota singolo A1/A3+A2 con scadenze, motore a tabella esplicita (approccio A, Turf per le intersezioni), AMSL mai convertito (warning), zone a schedule non filtrate. **Novità trasversale:** accordion+evidenziazione una-zona-alla-volta sostituirà anche il popup multi-zona di Fase 1. **Prossimo passo: review utente della spec → writing-plans.**
- **2026-07-03 (chiusura Fase 1)** — Fix flyTo (`c22285c`) approvato in re-review. **Review finale whole-branch (opus): Ready to merge, zero Critical/Important.** Applicato polish batch consigliato (`2b29d82`): popup con `textContent` (niente setHTML raw), `setErr(null)` dopo import riuscito, `geo.error` mostrato all'utente, listener `matchMedia` per il tema Sistema. Suite **34/34** e `tsc -b` verdi (verificati dal controller). Finding Minor residui = backlog Fase 2 nel ledger. **Prossimi passi:** verifica manuale E2E nel browser con l'utente → merge su `main` → deploy GitHub Pages → piano Fase 2.
- **2026-07-06 (popup raggruppato per nome zona)** — Lorenzo segnalava doppioni: le zone aeroportuali (es. "LIML_MILANO/LINATE 18/36") apparivano 6 volte nel popup. **Root cause confermato:** il file ED-269 di D-Flight spezza le zone in più record (bande di quota con floor/ceiling diversi, es. 0-120m, 25-120m, 45-120m) che condividono lo stesso nome ma hanno `identifier` diversi. 8336 feature → 2630 nomi distinti, 904 nomi con 6 record ciascuno. **Fix:** raggruppamento per nome nel popup (`plainGroupedZoneInfo`), una sola voce per zona con frase della fascia più restrittiva + fasce nei "Dettagli tecnici". Cambiato `highlightFilter` per usare `name` (non `id`) → illuminazione mappa copre tutti i gradoni. Cambiato `VerdictSheet` e `App` per passare nome come chiave. Suite 134/134 verdi, tsc pulito, build ok, E2E 15/15. **Committato `b5b258e`.**
- **2026-07-07 (evidenziazione per id, non per nome)** — Effetto collaterale del raggruppamento: `highlightFilter` per `name` illuminava TUTTI i gradoni della zona → macro-area rossa enorme al click. **Fix `4fc9da5`:** `highlightFilter` torna a usare `id`; il popup passa `bandId` (id della banda più restrittiva) a `onZoneFocus`, così si illumina solo quel poligono. Diff: `plainLanguage.ts` (`bandId` in `GroupedZoneInfo`), `popupContent.ts`, `App.tsx`, `VerdictSheet.tsx`, test aggiornati.
- **2026-07-07 (zone none invisibili)** — Lorenzo segnalava zone "inglobanti": le zone `none` (nessuna restrizione) hanno poligoni spesso enormi (corridoi, parchi nazionali) che con opacità 0.12 creavano un effetto visivo di "copertura" sulle zone più piccole. **Fix:** `ZONE_FILL_OPACITY.none → 0`, `ZONE_LINE_WIDTH.none → 0` — le zone `none` sono invisibili sulla mappa ma restano nel popup. Le zone con restrizioni restano visibili (prohibited 0.32, auth_required 0.24, conditional 0.18). Committato `70a26d0`.
- **2026-07-09 (ripresa: stato verificato + MEMORIA consolidata)** — Verificato: tutti i commit pushati, ultimi 3 deploy Pages **verdi** (`70a26d0` incluso), niente lock `.git/*.bak` residui. MEMORIA ripulita: rimosse le sezioni "RIPRESA"+"Log" **duplicate in coda** (introdotte per errore nel commit `4fc9da5`), log consolidato, chiusa la checklist pre-push. **Prossimo passo: decisioni A–D Fase 3 con Lorenzo.**
- **2026-07-09 notte (riprova offline → Task 8 cache tile)** — Prima verifica visiva di Lorenzo sulla preview: icona, installabilità e offline ok, ma "Offline non carica la mappa/cartina" → spiegato che era la decisione A (niente cache tile); Lorenzo ha scelto di riaprirla ("la mappa sì dai, anche solo dell'Italia"). **Decisione A RIVISTA nella spec** (opzione 2 originale): cache runtime **limitata** dei tile CARTO — `MAP_TILE_URL_RE` + route **CacheFirst** `carto-tiles-v1` (maxEntries 300, TTL 7gg, purgeOnQuotaError, cacheableResponse 0/200); copy OfflineBanner aggiornata ("ricerca non disponibile", la mappa ora c'è per le aree visitate); check E2E 3b sulla cache tile. Task 8 eseguito da subagent (brief `.superpowers/sdd/task-8-brief.md`), review Approved con 1 Important: docstring di `MAP_STYLE_URL_RE` rimasto alla decisione A vecchia → fixato dal controller (`dfbec97`). **Suite 166/166, tsc pulito, E2E offline 9/9, regressione 15/15.** Nota tecnica: le due regex sono strutturalmente disgiunte (tiles.json non può matchare la regex tile). In attesa della riprova di Lorenzo sulla preview (il reload online mostra il toast update dal vivo).
- **2026-07-09 sera (esecuzione Fase 3, subagent-driven)** — Eseguiti i 6 task implementativi del piano su `feat/fase3-offline-pwa` col metodo subagent-driven (implementer haiku/sonnet + task reviewer sonnet per task, review finale opus). Tutti Approved alla prima review; nessun difetto d'app trovato. **Fix di script/piano emersi:** (1) il check `includes('basemaps.cartocdn.com')` del piano sul sw.js era un falso negativo (workbox serializza la RegExp coi punti escaped); (2) nei test jsdom `new URL(rel, import.meta.url)` con readFileSync dà ENOENT → pattern `fileURLToPath+dirname+resolve`; (3) E2E offline: selettore giusto è `/^profilo$/i` (non "apri profilo") e serve viewport 1280×800 esplicito sul context (il default 720 spostava il click fuori dalle zone fixture). **Esiti:** suite **160/160**, `tsc -b` pulito, build ok (precache 14 entry, tile CARTO esclusi — verificato sulla regex serializzata), **E2E offline 8/8** (shell precacheata, banner, ricerca off, verdetto offline, toast aggiornamento col consenso, reload su build nuova) + **regressione 15/15**. Review finale opus: **Ready to merge**, Minor tutti a backlog (elenco nel TODO). Ledger aggiornato. **Merge/push in attesa della verifica di Lorenzo.**
- **2026-07-09 (Fase 3 sbloccata: decisioni + icona + spike + piano completo)** — Con Lorenzo: **decisioni A–D sciolte** (A: nessuna cache tile; B: spike; C: no bottone installa; D: **disegno nuovo**, diverso dalla raccomandazione). Tre proposte di icona presentate via artifact (https://claude.ai/code/artifact/e012502f-0670-4932-81db-900200c36247): **scelta "B — Zona sulla mappa"** (mappa notturna + zona rossa + drone bianco). **Spike decisione B eseguito subito sul Mac** (invece che in esecuzione — piano dimezzato): `vite-plugin-pwa@1.3.0` su rolldown-vite 8 builda pulito, precache 8 entry corretto (maplibre incluso, URL relativi allo scope), e **shell servita davvero offline** (Playwright, `setOffline` → reload → app ok; serve un reload online post-install perché il SW controlli la pagina). Verificati con curl gli host CARTO reali: **glyph/sprite/tiles.json stanno su `tiles.basemaps.cartocdn.com` come i TILE** → routing runtime cache per path (RegExp, non funzione: workbox serializza `urlPattern` e una closure si romperebbe). **Piano completato col codice per 7 task** (writing-plans + self-review; fix: variabili `mock*` per hoisting `vi.mock`, `fileURLToPath` per gli spazi nel path del progetto, estensione `.ts` negli import di vite.config sotto nodenext). Suite attesa a fine fase ~160 test + E2E `offline.mjs` 8 check. Residuo innocuo: `vite-plugin-pwa`+`playwright` in node_modules senza save (spike).
